These sets of files are to conglomerate the various database calls into single methods. 
I was having some issues with updating the program, due to having to pore over the entire app
editing various specific calls. This was my solution. None of these functions should be used without
requiring some form of authentication, whether via password and username, or via JWT token. 

                    -- Benjamin Deitch